pipeline {
    agent any

    stages {
        stage ("Build") {
            steps {
               echo "mvn -version"
               echo "clean install"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}