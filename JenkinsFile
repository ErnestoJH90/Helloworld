pipeline {
    agent any

    stages {
        stage ("Build") {
            steps {
                sh "mvn -version"
                echo "mvn clean install"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}