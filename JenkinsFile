pipeline {
    agent any

    tools{
        maven "3.6.0"
    }

    stages {
        stage('Build') {
            steps {
                bat 'mvn -version'
            }
        }
        stage('Install') {
            steps {
                bat 'mvn clean install'
            }
        }
        stage('Test') {
            steps {
                bat 'mvn test'
            }
        }
    }
        post {
         always {
            cleanWs()
    }
  }
}
