pipeline {
    agent any

    stages {
        stage ("Build") {
            steps {
               bat "mvn -version"
               
            }
        }
    }
        stage ("Install") {
            steps {
               
               bat "mvn clean install"
            }
        }

    post {
        always {
            cleanWs()
        }
    }
}