pipeline {
    agent any

    stages {
        stage ("Build") {
            steps {
               bat "mvn -version"
               
            }
        }
    }
    stages {
        stage ("Install") {
            steps {
               
               bat "mvn clean install"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}